# Task: S4-T08C Add smoke tests for bootstrap + endpoint

## Goal
Ensure CI validates that endpoint exists and notification call works.

## Files changed
- `infra/scripts/smoke_notify.sh`
- `.github/workflows/deploy.yml`
- `docs/plan/S4-T08C.md`

## What is implemented
- Smoke script validates required env vars (`NOTIFY_ENDPOINT`, `NOTIFY_API_KEY`).
- Request payload is built via `jq` with deterministic smoke metadata.
- Script checks:
  - HTTP status code equals `SMOKE_EXPECT_STATUS` (default `200`);
  - response JSON contains `ok=true`;
  - response JSON contains numeric `telegram_message_id`.
- Deploy workflow runs smoke step after bootstrap endpoint resolution and passes `NOTIFY_ENDPOINT` through environment.
- Dry-run path (`--dry-run`) is supported and used by CI when deploy is not executed.

## Acceptance criteria
- [x] Smoke test checks HTTP status + basic JSON shape.
- [x] Smoke test runs after deploy.

## Verification artifacts
- `infra/scripts/smoke_notify.sh --help`
- `NOTIFY_ENDPOINT=https://example.org/notify NOTIFY_API_KEY=dummy infra/scripts/smoke_notify.sh --dry-run`
- `python -m py_compile tools/yc_bootstrap.py`
